#include <string>
#include <vector>
#include <algorithm>
using namespace std;

vector<int> solution(int n, vector<vector<int>> point, vector<vector<int>> query) {
    vector<int> answer;
    sort(point.begin(), point.end());
    
    
    return answer;
}